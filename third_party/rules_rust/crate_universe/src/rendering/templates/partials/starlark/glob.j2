glob(
        include = [
            {%- for pattern in glob.include %}
            "{{ pattern }}",
            {%- endfor %}
        ],
        exclude = [
            {%- for pattern in glob.exclude %}
            "{{ pattern }}",
            {%- endfor %}
        ],
    )
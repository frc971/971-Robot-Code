namespace y2019;

table StatusLight {
  // How bright to make each one. 0 is off, 1 is full on.
  red:float;
  green:float;
  blue:float;
}

table CameraLog {
  log:bool;
}

root_type CameraLog;
root_type StatusLight;
